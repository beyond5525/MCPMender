{
  "name": "@mcpulse/cli",
  "version": "0.2.0",
  "type": "module",
  "description": "Cross-platform MCP configuration diagnostics, live handshake checks, and safe repair.",
  "license": "Apache-2.0",
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "mcpulse": "dist/mcpulse.cjs"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "dev": "node scripts/dev.mjs"
  },
  "devDependencies": {
    "@mcpulse/core": "workspace:*",
    "esbuild": "^0.25.5"
  }
}
